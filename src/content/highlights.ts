import type { Locale } from './i18n'

/**
 * 侧栏用的「这块的看点」：每块 2–4 条，一句话一个要点。
 * 地图页只加载这个文件，完整档案按需拉取。
 */
const ZH: Record<string, string[]> = {
  'cordis-core': [
    '服务解析走的是「只往上级找」的纤程游走，不是查全局表——这解释了一次真实事故的成因。',
    '任何会改变世界的动作都返回一张「退票」，卸载插件就是把退票反序执行一遍。',
    '加载顺序不是编排出来的，是从依赖声明里涌现的；就绪状态还记录「依赖由哪个实例提供」。',
  ],
  'cordis-loader': [
    '一行配置的四路 diff：只有 name / inject / group 三个字段的变化会强制重启，其余原地打补丁。',
    '多写一行 default 导出，会让整个插件的依赖声明被丢弃——事故催生了一条 AST 闸门。',
    '`!!js` 表达式只在两个字段上求值，且要等该行声明的依赖全部激活之后。',
  ],
  'agent-loop': [
    '回合 / 步 / 轮三层术语各有明确所指，不混用；整个驱动约 1500 行。',
    '工具并发执行但严格按模型期望的顺序提交；中断时给未执行的调用补写合成结果。',
    '三个取消来源融成一个信号，且在任何资源创建之前就注册好清理逻辑。',
  ],
  agent: [
    'Agent 接口和主循环是两个包：所有扩展依赖接口，所以换掉整个循环不波及生态。',
    '收件箱区分「下一回合」和「下一步」两个投放位，这是插话与注入的差别。',
    '每个 agent 有自己的注册作用域，同名的东西就近覆盖——一个会话可以有受限版工具。',
  ],
  session: [
    '模型历史是从只追加的日志算出来的，不是存在某个数组里。',
    '压缩靠追加一条遮蔽记录，不删原文；人类界面完全无视遮蔽，所以对话不会凭空消失。',
    '读到不认识的记录类型宁可拒绝重建整个会话，也不静默跳过。',
  ],
  tools: [
    '守卫的类型没有「放行」分支，于是组合律自动成立——注册顺序不影响结论。',
    '一次 execute 返回值拆成三种声明式投影：机器值、模型看的内容、界面卡片。',
    '展示函数软失败：日志比 schema 活得久，重放旧参数时绝不能让界面崩掉。',
  ],
  'system-prompt': [
    '段落按数字序号带排布：-100 身份、0 部署人格、100+ 工具指引。',
    '动态上下文是一条 user 角色的会话记录，不是系统提示词的隐藏后缀。',
    '变量插值严格失败：名字不合法、未注册、注册了但为空，一律抛错。',
  ],
  llm: [
    '适配器可以抛异常也可以发错误块，但消费者只会看到一种终止形态。',
    '规范化的边界画在语句粒度：消费者抛回生成器的异常不算 provider 故障。',
    '记录「哪些参数是适配器替你选的」，下一步精确删除它们重新解析。',
  ],
  fs: [
    '策略「决策」在事件层，策略「强制」在 provider 的原子临界区，两者都低于工具层。',
    '目标身份是不透明的品牌类型，文件不存在时用最近存在的祖先拼出稳定 key。',
    '路径包含判定先走词法快路径，不匹配时用 dev+ino 兜底对付短名与大小写别名。',
  ],
  shell: [
    '默认值必须显式落地成一份全部必填的规格，执行函数在类型上拒绝原始请求。',
    '「超时」「信号」「退出码」各占独立字段，绝不把一个嵌套进另一个的分支里。',
    '环境变量先 scrub 掉一切像凭据的名字，且 scrub 被导出成普通函数供够不到服务的调用方使用。',
  ],
  sandbox: [
    '交出 argv，换回替换 argv 加一份约束力报告：完全约束还是部分约束，是报告出来的。',
    '「沙箱本身坏了」必须优先于「命令被拒了」判定——前者意味着命令根本没运行。',
    '拒绝话术按后端各自匹配，不取并集；取并集会声称某后端产生不了的拒绝。',
  ],
  compaction: [
    '两种触发：请求前的压力检测，和上下文溢出错误后的恢复。',
    '只有在裁剪或摘要真正推进了「替换代数」时，才开一个新的重试回合。',
    '摘要作为遮蔽记录落进日志，因此压缩本身是纯粹的加法操作、永远可回溯。',
  ],
  subagent: [
    '一个接口装下三种物理形态的子代：进程内派生、进程内新建、外部命令行进程。',
    '能力不匹配就拒绝，绝不静默降级成一个丢掉了约束的执行。',
    '委派深度用「单调下界」而非从零计数，否则被恢复的子代能绕过深度上限。',
  ],
  'skills-web': [
    '技能目录进提示词，技能正文按需加载——上下文只为真正用到的那份付费。',
    '联网检索是四个 provider 的接缝，凭据以「引用」而非明文进入配置。',
    '外部工具名做成单射的纯函数：归一化有损时附加哈希，保证不会把两个工具合并成一个。',
  ],
  typert: [
    '构建期从 TypeScript 类型抽出类型图，两端各自生成校验器，网络上不传 schema。',
    'wire 字段名机械派生自声明，对不上就是构建失败。',
    '注册表保有「曾经存在过」的单调历史作为降级围栏，热卸载后拒绝回落到弱推断。',
  ],
  bridges: [
    '一份方言中立的内核加上各协议的薄桥：解码修一次，所有方言都受益。',
    '出口白名单写成一个被类型约束的常量数组，转发循环与订阅键集都从它派生。',
    'ACP、SDK、Claude Code / Codex 钩子共享同一套执行与合并策略。',
  ],
  'web-client': [
    '浏览器这一侧同样是 Cordis 插件树，界面模块以 ui-* 插件形式注册。',
    '面向浏览器的后端却是个 3744 行的手写巨石，没有注册接缝——与全仓其余部分相反。',
    '这块我只读了架构文档，没读实现，结论克制。',
  ],
  'patch-stack': [
    '启动时把根配置重写成空列表，真正的树来自一次扁平化的补丁应用。',
    'bundle 就是一个声明了补丁文件的 npm 包，组合器从不 import 它。',
    '配置查看命令复用装配时的同一个函数，靠前缀快照差分算出每行的来源层。',
  ],
  preset: [
    '一份预设只挂载一次，各会话通过作用域父子关系加入，而不是每会话挂一份。',
    '两道挂载期审计：不许有行卡在缺失服务上，不许有服务泄漏进根域。',
    '泄漏判定是结构性的而非启发式的——域内 provider 存在私有符号下，正确地不被误判。',
  ],
  'self-modification': [
    '模型可以定义、挂载、卸载自己写的插件；定义时就用运行时同一个编译器预检语法。',
    '沙箱的拒绝是「教学重定向」：错误信息直接指向该用的能力。',
    '模块文档第一句就承认这不是隔离——宿主辅助函数仍是逃逸路径。',
  ],
  engineering: [
    '生成器兼任过期检查器：加个参数在内存里重新生成、逐字节比对。',
    '分类型的检查失败闭合：遇到不认识的语法就报错，不静默放行。',
    '决策记录强制写「考虑过的替代方案」，归档区密码学冻结且明确不作为当前依据。',
  ],
  critique: [
    '那套漂亮的运行时断言，默认不在产品里跑——只在测试和 demo 里挂载。',
    '26 条接缝里 15 条从未换过实现；旗舰关系图的数据其实是手写的。',
    '存在四个包的循环依赖，而生成的依赖图按构造看不见它。',
  ],
}

/** 英文看点直接取自英文档案的 lessons，避免同一句话维护两处。 */
const EN: Record<string, string[]> = {}

export function highlightsOf(id: string, locale: Locale): string[] {
  return (locale === 'en' ? EN[id] : ZH[id]) ?? ZH[id] ?? []
}
